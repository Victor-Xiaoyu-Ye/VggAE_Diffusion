================================================================================
  VggAE-Diffusion 架构总览
================================================================================

                            ┌──────────────────────┐
                            │  mp4 video (8帧, 518²) │
                            └──────────┬───────────┘
                                       │
              ┌────────────────────────┴────────────────────────┐
              │               StreamVGGT Encoder                │
              │         (frozen, bf16, 24 alternating blocks)    │
              │                                                 │
              │   frame attn ←→ global attn ←→ frame attn ...   │
              │        1             2             3      ... 24 │
              └────────────────────────┬────────────────────────┘
                                       │
                    tokens_list[24] × [B,8,1+4+1369,2048]
                                       │
                              strip_special_tokens()
                                       │
                    tokens_list[24] × [B,8,1369,2048]
                                       │
                        ┌──────────────┴──────────────┐
                        │                             │
                   [4,11,17,23]                    [11]
                   (decoder)                   (diffusion)
                        │                             │
                        ▼                             ▼
              ┌─────────────────┐          ┌─────────────────┐
              │    Decoder       │          │   Diffusion      │
              │  (Phase 1 训练)   │          │  (Phase 2 训练)   │
              └─────────────────┘          └─────────────────┘


================================================================================
  Decoder — ViTDecoder (GLD channel-concat style)
================================================================================

  tokens_list at levels [4, 11, 17, 23]
  │
  │  level4:  [B, S, 1369, 2048]  ─┐
  │  level11: [B, S, 1369, 2048]  ─┤
  │  level17: [B, S, 1369, 2048]  ─┤─ torch.cat(dim=-1)
  │  level23: [B, S, 1369, 2048]  ─┘
  │
  ▼
  ┌────────────────────────────────────────────────────────────────┐
  │  [B, S, N=1369, 8192]                                         │
  │       │                                                        │
  │       ▼                                                        │
  │  Linear(8192 → 512)          ◄── GLD: channel-concat projection │
  │  + spatial pos embed [N, 512]                                  │
  │       │                                                        │
  │       ▼                                                        │
  │  ┌──────────────────────────────┐                              │
  │  │   Transformer Block × 8      │                              │
  │  │   ┌──────────────────────┐   │                              │
  │  │   │  LayerNorm            │   │                              │
  │  │   │  Self-Attention (8h)  │   │  dim=512                    │
  │  │   │  + residual           │   │  heads=8                    │
  │  │   ├──────────────────────┤   │  mlp_ratio=4                │
  │  │   │  LayerNorm            │   │                              │
  │  │   │  SwiGLU MLP (2048)    │   │                              │
  │  │   │  + residual           │   │                              │
  │  │   └──────────────────────┘   │                              │
  │  └──────────────────────────────┘                              │
  │       │                                                        │
  │       ▼                                                        │
  │  LayerNorm                                                     │
  │       │                                                        │
  │       ├── Linear(512 → 14×14×3) ──► RGB  [B,S,H,W,3]          │
  │       │                                Loss: L1 + LPIPS(1.0)    │
  │       │                                      + temporal + grad  │
  │       │                                                        │
  │       └── Linear(512 → 14×14)   ──► Depth [B,S,H,W]           │
  │                                        Loss: L1 (weight=0.1)   │
  └────────────────────────────────────────────────────────────────┘

  Decoder 鲁棒性增强 (训练时):
    • level_dropout=0.15     15% 概率丢弃非 boundary level → 输入变 0
    • boundary_only_prob=0.25  只用 boundary level (模拟 diffusion 输入)
    • token_noise_std=0.02   Gaussian 噪声
    → Linear(8192,512) 自然忽略零通道, 无需 mask token


================================================================================
  Diffusion — VideoDiT (OT-CFM flow matching)
================================================================================

  noise z₀ ~ N(0,I)  [B, 8, 1369, 2048]
  │
  │  OT-CFM:  zₜ = (1-t)·z₀ + t·z₁    ← z₁ = real boundary-level tokens
  │            v_target = z₁ - z₀
  │            v_pred = VideoDiT(zₜ, t, [text])
  │            loss = MSE(v_pred, v_target)
  │
  ▼
  ┌────────────────────────────────────────────────────────────┐
  │  ┌──────────────────────────────────────────────────────┐  │
  │  │                VideoDiT  (12 blocks)                  │  │
  │  │                                                      │  │
  │  │  zₜ [B, S, N, 2048]                                  │  │
  │  │       │                                              │  │
  │  │       ▼                                              │  │
  │  │  Input Proj  Linear(2048 → 768)                      │  │
  │  │  + sin-cos spatial pos embed [N, 768]                │  │
  │  │  + learned temporal pos embed [S, 768]               │  │
  │  │       │                                              │  │
  │  │       ▼                                              │  │
  │  │  ┌─────────────────────────────────────────────┐     │  │
  │  │  │  DiT Block × 12                             │     │  │
  │  │  │  ┌─────────────────────────────────────┐    │     │  │
  │  │  │  │  AdaLN: t → shift, scale, gate       │    │     │  │
  │  │  │  ├─────────────────────────────────────┤    │     │  │
  │  │  │  │  Spatial Self-Attention (12h, 768)   │    │     │  │
  │  │  │  │  ┌─────────────────────────────────┐ │    │     │  │
  │  │  │  │  │  QKV: [B×S, 1369, 768]          │ │    │     │  │
  │  │  │  │  │  Attn: softmax(QKᵀ/√d)·V        │ │    │     │  │
  │  │  │  │  └─────────────────────────────────┘ │    │     │  │
  │  │  │  ├─────────────────────────────────────┤    │     │  │
  │  │  │  │  (optional) Cross-Attn to CLIP text │    │     │  │
  │  │  │  │  ┌─────────────────────────────────┐ │    │     │  │
  │  │  │  │  │  Q: tokens, KV: text_emb [L,768] │    │     │  │
  │  │  │  │  └─────────────────────────────────┘ │    │     │  │
  │  │  │  ├─────────────────────────────────────┤    │     │  │
  │  │  │  │  Temporal Self-Attention (bidir)    │    │     │  │
  │  │  │  │  ┌─────────────────────────────────┐ │    │     │  │
  │  │  │  │  │  QKV: [B×1369, 8, 768]         │ │    │     │  │
  │  │  │  │  │  Attn: all frames attend each   │ │    │     │  │
  │  │  │  │  │  other (causal=False)           │ │    │     │  │
  │  │  │  │  └─────────────────────────────────┘ │    │     │  │
  │  │  │  ├─────────────────────────────────────┤    │     │  │
  │  │  │  │  SwiGLU MLP (3072)                  │    │     │  │
  │  │  │  │  + AdaLN gate                       │    │     │  │
  │  │  │  └─────────────────────────────────────┘    │     │  │
  │  │  └─────────────────────────────────────────────┘     │  │
  │  │       │                                              │  │
  │  │       ▼                                              │  │
  │  │  Output Proj  Linear(768 → 2048)  (zero-init)        │  │
  │  │  → v_pred [B, S, N, 2048]                            │  │
  │  └──────────────────────────────────────────────────────┘  │
  └────────────────────────────────────────────────────────────┘

  Diffusion 训练 trick:
    • input_noise=0.005        x₁ 加小噪声 (正则化)
    • EMA decay=0.9999         权重平滑
    • recon_every=1            Frozen decoder auxiliary loss (每步)
    • recon_weight=0.05         RGB-space 辅助监督
    • bf16 mixed precision
    • gradient checkpointing


================================================================================
  Inference — Sampling
================================================================================

  noise ~ N(0,I)
    │
    ▼
  ┌─────────────────────────────────────┐
  │  ODE Integration (Euler, 50 steps)   │
  │  z₀ → z₀ + v_pred·dt → ... → z₁     │
  │  (可选 CFG: v = v_uncond + 7.5×Δ)    │
  └─────────────────────────────────────┘
    │
    ▼
  build_decoder_tokens_from_generated(z₁, levels=[11])
    → tokens_list[24]×[B,8,1369,2048]  (other 23 levels = 0)
    │
    ▼
  ViTDecoder → RGB video [B,8,518,518,3]


================================================================================
  Loss 汇总
================================================================================

  Decoder:
    L = L1(RGB) + 1.0·LPIPS + 0.05·L_temporal + 0.05·L_grad + 0.1·L1(depth)

  Diffusion:
    L = MSE(v_pred, v_target) + 0.05·L1(recon_RGB)

================================================================================
